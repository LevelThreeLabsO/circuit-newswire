# Slack incoming webhook for the target channel. Start with a private JI channel;
# swap this secret to the real Circuit channel once the digest looks right.
# Paste it with no trailing whitespace — a stray newline fails identically to a
# revoked token, which cost a day once already.
SLACK_WEBHOOK_URL=
